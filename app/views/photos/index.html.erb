	<h1>All photos</h1>

	<ul class="thumbnails">
  <li class="span4">
    <a href="<%= link_to photo %>" class="thumbnail">
      <%= image_tag photo.file.url(:square) %>
    </a>
  </li>
  ...
</ul>


	<% @photos.each do |photo| %>
    
    <% end %>